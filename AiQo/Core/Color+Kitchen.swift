import SwiftUI

extension Color {
    static let aiqoBG = Color(.systemBackground)

    static let kitchenHeader = Color.black
    static let kitchenSubtitle = Color.gray

    static let kitchenMint = Color(red: 0.77, green: 0.94, blue: 0.86)

    static let kitchenButtonPrimary = Color(red: 1.0, green: 0.90, blue: 0.55) // قريب من الأصفر اللي بالصورة
}
