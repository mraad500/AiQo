<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- الأرقام الحقيقية من تطبيق صحة Salam OS (إذن بمستوى التوقيع). -->
    <uses-permission android:name="os.aiqo.health.permission.READ_METRICS" />
    <queries>
        <provider android:authorities="os.aiqo.health.metrics" />
    </queries>

    <application
        android:allowBackup="false"
        android:icon="@drawable/aiqo_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/aiqo_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.AiQo"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
